"""Multi-user web app: registration, Google login, CV upload, daily digests.

Wraps the ``job_radar`` engine with per-user auth, storage, and a daily worker.
Install with the optional extra: ``pip install -e '.[web]'``.
"""
